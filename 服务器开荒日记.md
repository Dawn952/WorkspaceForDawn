蓝区服务器，一个所有人梦寐以求的效率神器。但是真正拿到手后，面临的是一整个开荒的大动作，下面记录蓝区服务器开荒所面临的问题。

一、如何连接蓝区服务器

在上海/苏州/杭州等基地的Huawei-Guest的wifi下，默认防火墙已经与苏州的蓝区服务器打通。

此时要做的是申请一个vpn的白盒账号密码，此处为申请步骤。

在蓝区下载vpn，登录。

二、DNS配置

`ip link show`找到本机的网卡，找到如`enp210s0f0: <BROADCAST,MULTICAST,UP,LOWER_UP>`对应的一行（一般是第二行）

进入网卡对应的配置文件`vi /etc/sysconfig/network-scripts/ifcfg-enpxxx`，并在最下面加入一行`DSN1=8.8.8.8`

退出文件后重启网卡配置即可:`ifdown enpxxx && ifup enpxxx`

`curl http://www.baidu.com`来确认网口是否正确打开

三、安装NPU相关

4.1 下载

驱动和固件：
https://support.huawei.com/enterprise/zh/ascend-computing/ascend-hdk-pid-252764743/software
在上述网址中下载正式的HDK版本（需要根据提示下载ICSLite工具）
勾选Atlas-A3-aarch64，下载软件包Atlas-A3-hdk-npu-driver_26.0.rc1_linux-aarch64.run、Atlas-A3-hdk-npu-firmware_xxx.run

4.2 安装

安装指南参考：https://support.huawei.com/enterprise/zh/doc/EDOC1100570299/342a9f72

创建驱动运行用户HwHiAiUser（运行驱动进程的用户），安装驱动时无需指定运行用户，默认即为HwHiAiUser。

`groupadd HwHiAiUser`

`useradd -g HwHiAiUser -d /home/HwHiAiUser -m HwHiAiUser -s /bin/bash`

安装驱动源码编译所需依赖（OpenEuler系统）

`yum install -y make dkms gcc kernel-headers-$(uname -r) kernel-devel-$(uname -r) net-tools pciutils vim git `

将驱动包和固件包上传到服务器任意目录

给run包增加权限 `chmod +x *.run`

执行以下命令，完成驱动固件安装，软件包默认安装路径为“/usr/local/Ascend”。

首次安装场景，需按照"驱动->固件"的顺序安装驱动固件；覆盖安装场景，需按照“固件->驱动”的顺序安装固件驱动。

**安装驱动**

执行命令

`./Atlas-A3-hdk-npu-driver_<version>_linux-aarch64.run --full --install-for-all`

识别到`Driver package installed successfully!`即为安装成功，有任何问题请参考安装指南

**安装固件**

执行命令

`./Atlas-A3-hdk-npu-firmware_<version>.run --full`

识别到`Firmware package installed successfully! Reboot now or after driver installation for the installation/upgrade to take effect `即为安装成功

**重启系统**

执行命令`reboot`

等待重启后执行'npu-smi info'查看驱动加载是否成功

四、docker安装

有了连通外网的能力之后，当然是安装好docker来一键配齐能力

一键安装：`yum install -y docker`

启动docker：`systemctl start docker`

查看是否成功：`docker ps`

若显示以下内容则说明安装成功：
```shell
docker ps
CONTAINER ID        IMAGE               COMMAND             CREATED             STATUS              PORTS               NAMES
```

此时，可以登录https://quay.io/repository/ascend/vllm-ascend?tab=tags获取想用的开源镜像了，点击镜像的下载按钮以查看链接。

在服务器里执行如：`docker pull quay.io/ascend/vllm-ascend:v0.20.2rc1-a3`来获取镜像 注意：没有a3标识的是A2镜像，带a3标识的才是A3镜像。

三点一、docker启动脚本

`docker images`查询镜像id后，以自定义的容器name启动容器。
`bash start-docker.sh id name`

"start-docker.sh"
```bash
IMAGES_ID=$1
NAME=$2
if [ $# -ne 2 ]; then
    echo "error: need one argument describing your container name."
    exit 1
fi
docker run -it -d \
    --name ${NAME} \
    --shm-size=500g \
    --privileged=true \
    --net=host \
    -w /home \
    --device /dev/davinci0 \
    --device /dev/davinci1 \
    --device /dev/davinci2 \
    --device /dev/davinci3 \
    --device /dev/davinci4 \
    --device /dev/davinci5 \
    --device /dev/davinci6 \
    --device /dev/davinci8 \
    --device /dev/davinci9 \
    --device /dev/davinci10 \
    --device /dev/davinci11 \
    --device /dev/davinci12 \
    --device /dev/davinci13 \
    --device /dev/davinci14 \
    --device /dev/davinci15 \
    --device /dev/davinci_manager \
    --device /dev/devmm_svm \
    --device /dev/hisi_hdc \
    --entrypoint=bash \
    -v /usr/local/dcmi:/usr/local/dcmi \
    -v /usr/local/Ascend/driver/tools/hccn_tool:/usr/local/Ascend/driver/tools/hccn_tool \
    -v /usr/local/bin/npu-smi:/usr/local/bin/npu-smi \
    -v /usr/local/sbin:/usr/local/sbin \
    -v /usr/local/Ascend/driver/lib64/:/usr/local/Ascend/driver/lib64/ \
    -v /usr/local/Ascend/driver/version.info:/usr/local/Ascend/driver/version.info \
    -v /etc/ascend_install.info:/etc/ascend_install.info \
    -v /root/.cache:/root/.cache \
    -v /home:/home \
    -v /data:/data \
    -v /tmp:/tmp \
    -v /mnt:/mnt \
    $IMAGES_ID
```

三点二、下载权重（大工程）

在容器里执行以下脚本

```python
import os
from typing import Union
from modelscope.hub.api import HubApi
from modelscope.hub.snapshot_download import snapshot_download

# ==============================================================================
# 🚀 1. 并发下载优化 (核心优化点)
# ==============================================================================
# ModelScope SDK 默认并发数较低。通过设置环境变量，可以大幅提升大模型多文件下载的带宽利用率。
# 建议值：家庭宽带/普通服务器设为 4~8；高带宽服务器(如千兆内网/云服务器)可设为 16~32。
os.environ['MODELSCOPE_DOWNLOAD_PARALLEL'] = '16' 
# 同时可以设置超时时间，防止网络波动导致卡死（单位：秒）
os.environ['MODELSCOPE_DOWNLOAD_TIMEOUT'] = '300'

# ==============================================================================
# 🔐 2. 身份认证
# ==============================================================================
api = HubApi()
# 替换为你自己的 Token。使用 Token 登录可以避免匿名下载的速率限制，并允许访问私有模型。
api.login('ms-250b001e-2dd3-481a-abbd-5712c95340aa')

# ==============================================================================
# ⚙️ 3. 下载参数配置
# ==============================================================================
model_id = 'Eco-Tech/Kimi-K2.6-w4a8' 
local_dir = '/home/weights/kimi_k2.6_w4a8/' 

# --------------------------------------------------------------------------
# 🎯 关于 allow_patterns (白名单过滤) 的详细说明：
# --------------------------------------------------------------------------
# 意义：这是一个“白名单”机制。当模型仓库包含大量文件（例如 126 个权重分片、
# 多个不同精度的模型版本、无关的 PDF 文档等）时，你可以使用通配符 (*) 或正则表达式，
# **精确指定只下载你需要的特定文件**，从而节省 90% 以上的下载时间和磁盘空间。
#
# 💡 典型应用场景：
# 场景 A (完整下载 - 推荐用于生产)：
#   将 allow_patterns 设为 None 或注释掉，SDK 会下载仓库内的所有文件（包括 config.json, tokenizer 等必需文件）。
#
# 场景 B (仅下载部分权重用于本地测试)：
#   如果你只想下载前 2 个分片来测试代码能否跑通，可以写成：
#   allow_patterns = ["quant_model_weights-0000[1-2]-of-00126.safetensors", "config.json", "tokenizer.model"]
#
# 场景 C (你原脚本的写法)：
#   allow_patterns = ["quant_model_weights-0000*-of-00126.safetensors"]
#   这表示：只下载文件名匹配该模式的权重分片。**警告：这会漏掉 config.json 和 tokenizer，导致模型无法直接推理！**
# --------------------------------------------------------------------------

# 【请根据你的需求选择下面其中一种配置】

# 👉 选项 1：下载【完整模型】（包含所有权重分片 + config.json + tokenizer 等必需文件）
allow_patterns = None  
ignore_patterns = None # 如果你想排除某些大文件(如原始未量化权重)，可在此设置，例如: ["*.bin"]

# 👉 选项 2：仅下载【特定的几十个文件】用于快速测试（取消下面这行的注释，并注释掉选项1）
# allow_patterns = [
#     "config.json",                                  # 必须：模型结构配置
#     "tokenizer.model",                              # 必须：分词器
#     "quant_model_weights-00001-of-00126.safetensors", # 测试用：只下载第 1 个分片
#     "quant_model_weights-00002-of-00126.safetensors"  # 测试用：只下载第 2 个分片
# ]

# ==============================================================================
# 📥 4. 执行下载
# ==============================================================================
print(f"🚀 开始下载模型: {model_id}")
print(f"💾 保存路径: {local_dir}")
print(f"⚡ 当前并发数: {os.environ.get('MODELSCOPE_DOWNLOAD_PARALLEL')}")

try:
    model_dir = snapshot_download(
        model_id=model_id,
        allow_patterns=allow_patterns,
        ignore_patterns=ignore_patterns,
        local_dir=local_dir,
        # cache_dir 默认在 ~/.cache/modelscope，如果 /mnt/share 空间不够，可取消注释并指定其他大盘路径
        # cache_dir='/mnt/large_disk/modelscope_cache/' 
    )
    print(f"✅ 下载成功完成！模型已保存至: {model_dir}")
    
except Exception as e:
    print(f"❌ 下载过程中发生错误: {e}")
    print("💡 提示：ModelScope 支持断点续传，修复网络或配置后，直接重新运行此脚本即可。")
```

**暂时就到这里，持续更新
